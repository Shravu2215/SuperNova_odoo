# Copy this file, rename the copy to .env
# For local development these defaults are already correct:
VITE_API_URL=http://localhost:5000/api
VITE_JWT_STORAGE_KEY=assetflow_jwt
VITE_WS_URL=http://localhost:5000
